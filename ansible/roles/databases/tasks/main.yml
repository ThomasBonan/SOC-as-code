---
- name: Add Bitnami repo (once)
  kubernetes.core.helm_repository:
    name: "{{ bitnami_repo_name }}"
    repo_url: "{{ bitnami_repo_url }}"
    binary_path: "{{ helm_binary_path }}"
  delegate_to: localhost
  run_once: true
  become: false

- name: Update helm repos (once)
  ansible.builtin.command: "{{ helm_binary_path }} repo update"
  delegate_to: localhost
  run_once: true
  become: false

- block:
    - name: Ensure namespace exists
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: present
        definition:
          apiVersion: v1
          kind: Namespace
          metadata:
            name: "{{ soc_core_namespace }}"

    # -------------------------
    # TheHive PostgreSQL secret
    # -------------------------
    - name: Check TheHive PostgreSQL secret exists
      kubernetes.core.k8s_info:
        kubeconfig: "{{ kubeconfig_path }}"
        api_version: v1
        kind: Secret
        name: thehive-postgresql-auth
        namespace: "{{ soc_core_namespace }}"
      register: thehive_pg_secret
      vars:
        ansible_python_interpreter: "{{ k8s_ansible_python }}"

    - name: Create TheHive PostgreSQL secret (create-once)
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: present
        definition:
          apiVersion: v1
          kind: Secret
          metadata:
            name: thehive-postgresql-auth
            namespace: "{{ soc_core_namespace }}"
          type: Opaque
          stringData:
            postgres-password: "{{ lookup('password', '/dev/null length=32 chars=ascii_letters,digits') }}"
            password: "{{ lookup('password', '/dev/null length=32 chars=ascii_letters,digits') }}"
      when:
        - (thehive_pg_secret.resources | length) == 0
        - soc_vault_backend | default('kubernetes-secrets') == 'kubernetes-secrets'

    # -------------------------
    # Cortex PostgreSQL secret
    # -------------------------
    - name: Check Cortex PostgreSQL secret exists
      kubernetes.core.k8s_info:
        kubeconfig: "{{ kubeconfig_path }}"
        api_version: v1
        kind: Secret
        name: cortex-postgresql-auth
        namespace: "{{ soc_core_namespace }}"
      register: cortex_pg_secret
      vars:
        ansible_python_interpreter: "{{ k8s_ansible_python }}"

    - name: Create Cortex PostgreSQL secret (create-once)
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: present
        definition:
          apiVersion: v1
          kind: Secret
          metadata:
            name: cortex-postgresql-auth
            namespace: "{{ soc_core_namespace }}"
          type: Opaque
          stringData:
            postgres-password: "{{ lookup('password', '/dev/null length=32 chars=ascii_letters,digits') }}"
            password: "{{ lookup('password', '/dev/null length=32 chars=ascii_letters,digits') }}"
      when:
        - (cortex_pg_secret.resources | length) == 0
        - soc_vault_backend | default('kubernetes-secrets') == 'kubernetes-secrets'

    # -------------------------
    # MISP MariaDB secret
    # -------------------------
    - name: Check MISP MariaDB secret exists
      kubernetes.core.k8s_info:
        kubeconfig: "{{ kubeconfig_path }}"
        api_version: v1
        kind: Secret
        name: misp-mariadb-auth
        namespace: "{{ soc_core_namespace }}"
      register: misp_mariadb_secret
      vars:
        ansible_python_interpreter: "{{ k8s_ansible_python }}"

    - name: Create MISP MariaDB secret (create-once)
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: present
        definition:
          apiVersion: v1
          kind: Secret
          metadata:
            name: misp-mariadb-auth
            namespace: "{{ soc_core_namespace }}"
          type: Opaque
          stringData:
            mariadb-root-password: "{{ lookup('password', '/dev/null length=32 chars=ascii_letters,digits') }}"
            mariadb-password: "{{ lookup('password', '/dev/null length=32 chars=ascii_letters,digits') }}"
      when:
        - (misp_mariadb_secret.resources | length) == 0
        - soc_vault_backend | default('kubernetes-secrets') == 'kubernetes-secrets'

  delegate_to: localhost
  run_once: true
  become: false
  vars:
    ansible_python_interpreter: "{{ k8s_ansible_python }}"

# --- Vault backend : lire les passwords depuis Vault KV et créer les K8s Secrets directement ---
- block:
    - name: Databases (Vault) | Lire le root token
      kubernetes.core.k8s_info:
        kubeconfig: "{{ kubeconfig_path }}"
        kind: Secret
        namespace: "{{ soc_vault_namespace | default('soc-vault') }}"
        name: "vault-unseal-keys"
      register: _db_vault_keys
      no_log: true
      failed_when: _db_vault_keys.resources | length == 0

    - name: Databases (Vault) | Construire préfixe vault exec
      ansible.builtin.set_fact:
        _db_vault_exec: >-
          kubectl exec -n {{ soc_vault_namespace | default('soc-vault') }} vault-0 --
          env VAULT_TOKEN={{ _db_vault_keys.resources[0].data.root_token | b64decode }}
      no_log: true

    - name: Databases (Vault) | Lire thehive-postgresql depuis Vault
      ansible.builtin.shell: >
        {{ _db_vault_exec }} vault kv get -format=json
        {{ soc_vault_kv_mount | default('soc') }}/databases/thehive-postgresql
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      register: _db_thehive_pg_vault
      no_log: true
      changed_when: false
      failed_when: _db_thehive_pg_vault.rc != 0

    - name: Databases (Vault) | Créer thehive-postgresql-auth (create-once)
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: present
        definition:
          apiVersion: v1
          kind: Secret
          metadata:
            name: thehive-postgresql-auth
            namespace: "{{ soc_core_namespace }}"
          type: Opaque
          stringData:
            postgres-password: "{{ (_db_thehive_pg_vault.stdout | from_json).data.data['postgres-password'] }}"
            password: "{{ (_db_thehive_pg_vault.stdout | from_json).data.data['password'] }}"
      when: (thehive_pg_secret.resources | length) == 0
      no_log: true

    - name: Databases (Vault) | Lire cortex-postgresql depuis Vault
      ansible.builtin.shell: >
        {{ _db_vault_exec }} vault kv get -format=json
        {{ soc_vault_kv_mount | default('soc') }}/databases/cortex-postgresql
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      register: _db_cortex_pg_vault
      no_log: true
      changed_when: false
      failed_when: _db_cortex_pg_vault.rc != 0

    - name: Databases (Vault) | Créer cortex-postgresql-auth (create-once)
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: present
        definition:
          apiVersion: v1
          kind: Secret
          metadata:
            name: cortex-postgresql-auth
            namespace: "{{ soc_core_namespace }}"
          type: Opaque
          stringData:
            postgres-password: "{{ (_db_cortex_pg_vault.stdout | from_json).data.data['postgres-password'] }}"
            password: "{{ (_db_cortex_pg_vault.stdout | from_json).data.data['password'] }}"
      when: (cortex_pg_secret.resources | length) == 0
      no_log: true

    - name: Databases (Vault) | Lire misp-mariadb depuis Vault
      ansible.builtin.shell: >
        {{ _db_vault_exec }} vault kv get -format=json
        {{ soc_vault_kv_mount | default('soc') }}/databases/misp-mariadb
      environment:
        KUBECONFIG: "{{ kubeconfig_path }}"
      register: _db_misp_mariadb_vault
      no_log: true
      changed_when: false
      failed_when: _db_misp_mariadb_vault.rc != 0

    - name: Databases (Vault) | Créer misp-mariadb-auth (create-once)
      kubernetes.core.k8s:
        kubeconfig: "{{ kubeconfig_path }}"
        state: present
        definition:
          apiVersion: v1
          kind: Secret
          metadata:
            name: misp-mariadb-auth
            namespace: "{{ soc_core_namespace }}"
          type: Opaque
          stringData:
            mariadb-root-password: "{{ (_db_misp_mariadb_vault.stdout | from_json).data.data['mariadb-root-password'] }}"
            mariadb-password: "{{ (_db_misp_mariadb_vault.stdout | from_json).data.data['mariadb-password'] }}"
      when: (misp_mariadb_secret.resources | length) == 0
      no_log: true

  when: soc_vault_backend | default('kubernetes-secrets') == 'vault'
  delegate_to: localhost
  run_once: true
  become: false
  vars:
    ansible_python_interpreter: "{{ k8s_ansible_python }}"

# --- DB Deployments ---
- name: Check Longhorn system is healthy before DBs
  ansible.builtin.command: >
    kubectl --kubeconfig {{ kubeconfig_path }} -n longhorn-system get pods
  register: lh_pods
  changed_when: false
  delegate_to: localhost
  run_once: true

- name: Fail if Longhorn managers are not Running
  ansible.builtin.fail:
    msg: "Longhorn not healthy:\n{{ lh_pods.stdout }}"
  when: "'longhorn-manager' in lh_pods.stdout and 'Running' not in lh_pods.stdout"
  delegate_to: localhost
  run_once: true

- name: Deploy TheHive PostgreSQL
  kubernetes.core.helm:
    name: thehive-postgresql
    chart_ref: bitnami/postgresql
    chart_version: "{{ postgres_chart_version }}"
    release_namespace: "{{ soc_core_namespace }}"
    create_namespace: true
    kubeconfig: "{{ kubeconfig_path }}"
    values_files:
      - "{{ role_path }}/files/values/thehive-postgresql.values.yml"
    wait: true
    atomic: true
    timeout: "30m0s"
    binary_path: "{{ helm_binary_path }}"
  delegate_to: localhost
  run_once: true

- name: Wait for TheHive PostgreSQL pod to be Ready
  kubernetes.core.k8s_info:
    kubeconfig: "{{ kubeconfig_path }}"
    kind: Pod
    namespace: "{{ soc_core_namespace }}"
    label_selectors:
      - "app.kubernetes.io/instance=thehive-postgresql"
  register: thehive_pg_pods
  delegate_to: localhost
  run_once: true
  become: false
  vars:
    ansible_python_interpreter: "{{ k8s_ansible_python }}"

- name: Fail if TheHive PostgreSQL pod not Ready
  ansible.builtin.fail:
    msg: "TheHive PostgreSQL pods not ready: {{ thehive_pg_pods.resources | map(attribute='status.phase') | list }}"
  when: >
    (thehive_pg_pods.resources | length) == 0 or
    (thehive_pg_pods.resources | selectattr('status.containerStatuses', 'defined') | list | length) == 0 or
    (thehive_pg_pods.resources | selectattr('status.containerStatuses', 'defined')
       | map(attribute='status.containerStatuses') | list | first
       | selectattr('ready','equalto',true) | list | length) == 0

- name: Deploy Cortex PostgreSQL
  kubernetes.core.helm:
    name: "{{ cortex_pg_release }}"
    chart_ref: "{{ postgres_chart_ref }}"
    chart_version: "{{ postgres_chart_version }}"
    release_namespace: "{{ soc_core_namespace }}"
    create_namespace: false
    kubeconfig: "{{ kubeconfig_path }}"
    values_files:
      - "{{ role_path }}/files/values/cortex-postgresql.values.yml"
    wait: true
    atomic: true
    timeout: "30m0s"
    binary_path: "{{ helm_binary_path }}"
  delegate_to: localhost
  run_once: true
  become: false

- name: Deploy MISP MariaDB
  kubernetes.core.helm:
    name: "{{ misp_db_release }}"
    chart_ref: "{{ mariadb_chart_ref }}"
    chart_version: "{{ mariadb_chart_version }}"
    release_namespace: "{{ soc_core_namespace }}"
    create_namespace: false
    kubeconfig: "{{ kubeconfig_path }}"
    values_files:
      - "{{ role_path }}/files/values/misp-mariadb.values.yml"
    wait: true
    atomic: true
    timeout: "30m0s"
    binary_path: "{{ helm_binary_path }}"
  delegate_to: localhost
  run_once: true
  become: false

