## Exemple à intégrer à proxmox pour la création d'une VM Ubuntu 24.04 avec cloud-init

#cloud-config
package_update: true
packages:
  - qemu-guest-agent

runcmd:
  - systemctl enable --now qemu-guest-agent
  - sed -i '/ swap / s/^/#/' /etc/fstab || true
  - swapoff -a || true
  - sysctl -w net.ipv4.ip_forward=1