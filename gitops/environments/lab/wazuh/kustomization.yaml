apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Phase 6 GitOps — Wazuh config (ConfigMaps + Ingress)
# Helm release, Secrets, StatefulSet patch et bootstrap restent Ansible (Day 1)
# Les ressources K8s Day 2 sont gérées ici ; Ansible les saute via _managed_by=gitops
# NB: wazuh-blackbox-probes supprimé — doublon cassé (mauvais DNS wazuh-core-cluster)
# du probe Helm wazuh.soc-wazuh.svc:55000 qui fonctionne (cf monitoring_blackbox_targets).
resources:
  - configmap-wazuh-thehive-integration.yaml
  - configmap-siem-connector-stub.yaml
  - ingress-wazuh.yaml
