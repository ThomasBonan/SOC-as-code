# configmap-cortex-connector-stub.yaml
# Phase 6b GitOps — Cortex config
# Migré depuis soc_foundations Ansible role (integration_placeholders.yml)
# Stub de configuration pour le connecteur externe Cortex (futur)
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: soc-cortex-connector-stub
  namespace: soc-cortex
  labels:
    app.kubernetes.io/managed-by: soc-gitops
    soc.lab/component: integration-stub
    soc.lab/connector-type: analyzer-config
    soc.lab/target-service: cortex
  annotations:
    soc.lab/created-by: soc-gitops Phase 6b
    soc.lab/description: Placeholder for future Cortex external connector configuration
    soc.lab/ready: "false"
    soc.lab/status: placeholder
    soc.lab/phase: "6b"
data:
  config: |
    # Placeholder de configuration pour soc-cortex-connector-stub
    # Remplacer ce stub par la configuration réelle du connecteur.
    # connector_type : analyzer-config
    # target_service : cortex
    #
    # Étapes pour activer :
    #   1. Remplir les clés de configuration ci-dessous
    #   2. Mettre enabled: "true"
    #   3. Mettre l'annotation soc.lab/ready: "true"
    endpoint: ""
    auth_secret_ref: ""
    enabled: "false"
  connector_type: analyzer-config
  description: Placeholder for future Cortex external connector configuration
  enabled: "false"
  status: placeholder
  target_service: cortex
