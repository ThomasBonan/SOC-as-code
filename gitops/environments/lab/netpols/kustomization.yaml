apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Phase 3 GitOps — NetworkPolicies applicatives
# Helm (Wazuh, TheHive, Cortex, MISP, Vault, ArgoCD) reste géré par Ansible (Day 1)
# Les NetworkPolicies Day 2 sont gérées ici ; Ansible les saute via _managed_by=gitops
resources:
  - netpol-wazuh.yaml
  - netpol-thehive.yaml
  - netpol-cortex.yaml
  - netpol-misp.yaml
  - netpol-vault.yaml
  - netpol-argocd.yaml
