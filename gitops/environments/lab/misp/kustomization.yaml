apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Phase 5 GitOps — MISP config (ConfigMap + Ingress)
# Helm Redis, Deployment misp-core/misp-modules, Secrets et bootstrap restent Ansible (Day 1)
# Les ressources K8s Day 2 sont gérées ici ; Ansible les saute via _managed_by=gitops
resources:
  - configmap-misp-config.yaml
  - configmap-misp-feed-stub.yaml
  - ingress-misp.yaml
