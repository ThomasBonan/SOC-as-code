apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: thehive-selftest-foundations
  namespace: soc-thehive
  labels:
    app.kubernetes.io/managed-by: soc-gitops
    soc.lab/component: eso-external-secret
  annotations:
    soc.lab/vault-path: "service-accounts/thehive-selftest-sa"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: thehive-selftest-foundations
    creationPolicy: Owner
  data:
    - secretKey: api_key
      remoteRef:
        key: service-accounts/thehive-selftest-sa
        property: api_key
    - secretKey: login
      remoteRef:
        key: service-accounts/thehive-selftest-sa
        property: login
