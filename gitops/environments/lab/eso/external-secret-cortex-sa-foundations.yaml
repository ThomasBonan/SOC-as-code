apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cortex-sa-foundations
  namespace: soc-cortex
  labels:
    app.kubernetes.io/managed-by: soc-gitops
    soc.lab/component: eso-external-secret
  annotations:
    soc.lab/vault-path: "service-accounts/cortex-sa"
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: cortex-sa-foundations
    creationPolicy: Owner
  data:
    - secretKey: api_key
      remoteRef:
        key: service-accounts/cortex-sa
        property: api_key
    - secretKey: login
      remoteRef:
        key: service-accounts/cortex-sa
        property: login
